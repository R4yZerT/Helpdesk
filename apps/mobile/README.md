# apps/mobile

Aplicacion movil (Expo / React Native + TypeScript).
Prototipo: estructura vacia, se implementa en la siguiente etapa.

> Comparte tipos y contratos con `shared/` y la web via `react-native-web`.